<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<sqlMap namespace="com.logisticscenter.mapper.CompulsoryDao">
	<typeAlias type="com.entity.CompulsoryEntity" alias="compulsoryE"/>
	<resultMap id="compulsoryMap" class="com.entity.CompulsoryEntity" >
		<result column="id" property="id" jdbcType="INT" />
		<result column="truckNumber" property="truckNumber" jdbcType="VARCHAR" />
		<result column="startDate" property="startDate" jdbcType="VARCHAR"/>
		<result column="endDate" property="endDate" jdbcType="VARCHAR"/>
		<result column="createDate" property="createDate" jdbcType="VARCHAR" />
		<result column="createTime" property="createTime" jdbcType="VARCHAR" />
		<result column="editDate" property="editDate" jdbcType="VARCHAR" />
		<result column="editTime" property="editTime" jdbcType="VARCHAR" />
	</resultMap>
  
  <!-- 获得全查询列表 -->
  <select id="getAllCompulsory" resultMap="compulsoryMap">
    select * from compulsoryInsurance
  </select>
  
   <!--  -->
   <select id="getCompulsoryBy" resultMap="compulsoryMap" parameterClass="compulsoryE">
	select top $pageSize$ * from ( select row_number() over(order by id desc) as rownumber,* from compulsoryInsurance 
	<dynamic prepend="WHERE">
		<isNotEmpty prepend="AND" property="truckNumber">
			truckNumber like '%' + #truckNumber# +  '%' 
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="startDate">
			startDate &gt; #startDate#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="endDate">
			endDate &lt; #endDate#
		</isNotEmpty>
	</dynamic>
	) A
	<dynamic prepend="WHERE ">
		<isNotEmpty prepend="AND" property="currentPage">
			rownumber > $currentPage$
		</isNotEmpty>
	</dynamic>
     
  </select> 
  
   <!-- 获得分页的count -->
   <select id="getCompulsoryByCount" resultClass="String" parameterClass="compulsoryE">
	select count(*) from ( select row_number() over(order by id) as rownumber,* from compulsoryInsurance ) A
	<dynamic prepend="WHERE">
		<isNotEmpty prepend="AND" property="truckNumber">
			truckNumber like '%' + #truckNumber# +  '%' 
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="startDate">
			startDate &gt; #startDate#
		</isNotEmpty>
		<isNotEmpty prepend="AND" property="endDate">
			endDate &lt; #endDate#
		</isNotEmpty>
	</dynamic>
     
  </select> 
  
  <!-- 根据id获得车辆商业险 -->
    <select id="getCompulsoryById" resultMap="compulsoryMap">
     select * from compulsoryInsurance where id=#id#
  </select> 
  
   <!-- 新增车辆商业险 -->
   <insert id="insertCompulsory" parameterClass="compulsoryE">
      insert into compulsoryInsurance (truckNumber,startDate,endDate,createDate,createTime,editDate,editTime) 
      values (#truckNumber#,#startDate#,#endDate#,#createDate#,#createTime#,#editDate#,#editTime#)
      <selectKey resultClass="int" keyProperty="id" type="post">    
			SELECT max(id) as id from compulsoryInsurance
		</selectKey>
   </insert>
   
   <!-- 删除车辆商业险 -->
   <delete id="deleteCompulsory" parameterClass="java.util.List">
     delete from compulsoryInsurance 
	<iterate prepend="WHERE" open="(" close=")"
	conjunction="OR">
	id=#idLst[]#
	</iterate>
   </delete>
   
   <!-- 更新车辆商业险类型 -->
   <update id="updateCompulsory" parameterClass="compulsoryE">
      update compulsoryInsurance set truckNumber=#truckNumber#,startDate=#startDate#,endDate=#endDate#,editDate=#editDate#,editTime=#editTime# where id=#id#
   </update>
    <!-- 根据id获得车辆商业险 -->
    <select id="getWarnCompulsory" resultMap="compulsoryMap" parameterClass="String">
     select * from compulsoryInsurance where (startDate &lt; $days$ and enddate &gt; $days$)
  </select> 
</sqlMap>